version: 0.2

phases:
  install:
    runtime-versions:
      golang: latest
    commands:
      - cd site && npm install
  build:
    commands:
      - cd site && npm run build --production
  post_build:
    commands:
      - ls
      - cd site && rm -rf node_modules

artifacts:
  files:
    - '**/*'
